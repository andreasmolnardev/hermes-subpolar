# Git
.git
.gitignore
.gitmodules

# Dependencies
node_modules
**/node_modules

# Built browser artifacts are regenerated inside the image.
packages/web-ui/dist/

# CI/CD
.github

# Environment files
.env
.env.*

# IDE
.vscode/
.idea/
*.swp
*.swo

# Documentation
*.md

# Runtime data (bind-mounted at /opt/data; must not leak into build context)
data/
.hermes-docker/

# Compose/profile runtime state (bind-mounted; avoid ownership/secret issues)
hermes-config/
runtime/

# ---------- Not needed inside the Docker image ----------

# Desktop and terminal clients are not shipped in Docker.
apps/
ui-tui/

# Test suite — not shipped in production images
tests/

# Supplementary docs
website/
docs/

# Assets only used by the GitHub README
assets/
infographic/

# Plugin-level docs (hermes-achievements ships docs/ but the runtime doesn't read them)
plugins/hermes-achievements/docs/

packaging/

# Design and planning documents
plans/
.plans/

# Repo-level dotfiles that are git-only or dev-tooling config
.env.example
.envrc
.gitattributes
.hadolint.yaml
.mailmap
