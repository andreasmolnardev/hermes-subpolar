# Windows Docker Desktop override for the same single Subpolar service.
# Configure auth variables in .env or the invoking shell before starting.
services:
  subpolar:
    ports:
      - "127.0.0.1:9119:9119"
