{
  "name": "subpolar-server",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "exports": { ".": "./src/server.ts" },
  "bin": { "subpolar": "./src/cli.ts" },
  "dependencies": {
    "api-gateway": "0.0.0",
    "chat-provider-interface": "0.0.0",
    "data-layer": "0.0.0"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "bun test test",
    "check": "bun run typecheck && bun run test",
    "serve": "bun src/cli.ts"
  },
  "devDependencies": {
    "@types/bun": ">=1.3.0 <1.4.0"
  }
}
